package app.model.event;

/** Origin of an enriched semantic observation. */
public enum SemanticProvenance {
    ARENA_TARGET_DECLARATION,
    ARENA_CATEGORY,
    ZONE_PATTERN,
    UNIQUE_TARGET_CORRELATION
}
