--- a/src/main/java/app/projection/TokenResolver.java
+++ b/src/main/java/app/projection/TokenResolver.java
@@ -45,7 +45,9 @@
             }
         }
 
-        if (best != null && bestScore >= 20 && bestScore - secondBestScore >= 5) {
+        boolean hasNoRunnerUp = secondBestScore == Integer.MIN_VALUE;
+        boolean isClearlyBetter = hasNoRunnerUp || bestScore - secondBestScore >= 5;
+        if (best != null && bestScore >= 20 && isClearlyBetter) {
             return best;
         }
         return null;
